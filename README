-=-=- XSD to SAX Parser generator -=-=-

Build:
  This project uses Maven 2
    > mvn clean package

Run:
  The build phase produces several distribution packages
  under *target/*
  - Xsd2SaxParser-*-distrib.zip
  - Xsd2SaxParser-*-distrib.tar.gz
  - Xsd2SaxParser-*-distrib (uncompressed distribution directory)
  A *generate.bat* and *generate.sh* are provided to launch the Java
  runtime.
    > generate
  will show the usage of the command:
  usage: generate [options] <XSD file> ...
 -c,--passer-class-name <class-name>   class name for the generated parser
 -n,--no-slf4j                         do not use slf4j logging in parser
 -o,--outout-dir <directory>           output directory for the generated
                                       parser source
 -p,--package <package-name>           pakcage name for generated parser
 -t,--template-file <template-file>    alternate template file for SAX
                                       parser
 -v,--verbose                          display verbose traces

License:
  LGPL 2.1


